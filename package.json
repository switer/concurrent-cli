{
  "name": "concurrent-cli",
  "version": "0.1.4",
  "description": "Cli tool use for concurrent command running",
  "author": {
    "name": "switer"
  },
  "scripts": {
    "test": "node test/bin.test",
    "deploy": "dos2unix lib/cli-wrapper.js & npm publish"
  },
  "repository": {
    "type": "git",
    "url": "git@github.com:switer/concurrent-cli.git"
  },
  "bugs": {
    "url": "https://github.com/switer/concurrent-cli/issues"
  },
  "homepage": "https://github.com/switer/concurrent-cli",
  "main": "lib/concurrent-cli.js",
  "bin": {
    "concurrent-cli": "lib/cli-wrapper.js"
  },
  "engines": {
    "node": ">=0.6.0"
  },
  "dependencies": {
    "colors": "^0.6.2"
  }
}
